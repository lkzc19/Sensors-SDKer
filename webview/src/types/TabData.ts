export interface BaseTabData {
  event: string;
  time: string;
  idm: string;
  distinct_id: string;
  login_id: string;
}

export interface OtherTabData {
  index: number;
  pKey: string;
  pValue: string;
  pType: string;
}